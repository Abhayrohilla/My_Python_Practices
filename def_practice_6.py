# 6. Write a Python function to check whether a number is in a given range.
def fun(x):
        if x in range(1,9):
            print(f"{x} is present in function")
        else:
            print(f"{x} is not present in function")
fun(5)